# Work flow

**1.**
Edit code

**2.**
Run `pnpm test`

**3.**
Build the code `pnpm build`
_Don't foget to bump version_

**4.**
Run `npm publish --dry-run` to check publish files.

Then run `npm publish` to publish the code.
